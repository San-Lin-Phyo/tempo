// s. trowbridge 2024
#include <iostream>
using namespace std;

int main() {
    cout << endl;

    int x;
    cout << "Enter x: ";
    cin >> x;

    cout << ( x % 2 == 0 ) << "\n"; // an expression to test if x is even

    cout << ( x % 2 != 0 ) << "\n"; // an expression to test if x is odd

    cout << endl;
    return 0;
}
